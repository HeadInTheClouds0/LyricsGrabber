<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.headintheclouds.lyricsgrabber">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppLyricsGrabber"
        android:allowBackup="true"
        android:enabled="true"
        android:icon="@drawable/baseline_queue_music_white_48"
        android:label="@string/app_name"
        android:roundIcon="@drawable/baseline_queue_music_white_48"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".activities.LyricsActivity" />
        <activity android:name=".activities.LyricsListActivity" />
        <activity
            android:name=".activities.ThemeSwitcherActivity"
            android:label="@string/title_activity_theme_switcher"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>